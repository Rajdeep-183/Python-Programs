class Solution:
    def sortedSquares(self, nums: List[int]) -> List[int]:
        s = []
        for i in nums:
            a = i**2
            s.append(a)
        
        p = sorted(s)
        return p
